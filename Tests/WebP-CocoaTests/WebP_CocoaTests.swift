import XCTest
@testable import WebP_Cocoa

final class WebP_CocoaTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WebP_Cocoa().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
